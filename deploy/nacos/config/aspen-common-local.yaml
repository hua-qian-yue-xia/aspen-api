# Aspen 公共配置 (dataId: aspen-common-local.yaml, Group: DEFAULT_GROUP)
# 分层: 代码内安全默认值 < 本文件 < {service}-biz-local.yaml < 受控环境变量/Secret
# 只承载全服务共享的本地环境配置; 服务私有配置放各自的 {service}-biz-local.yaml

aspen:
  routes:
    # 路由分发的 Redis 隔离段, 全服务共享, 必须与代码默认值一致
    environment: local
